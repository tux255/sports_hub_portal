<%= form_for(post, class:'border p-2') do |f| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field-group mb-2">
    <%= f.file_field-group mb-2 :avatar, class: 'form-control' %>
  </div>

  <div class="field-group mb-2">
    <%= f.label :title %>
    <%= f.text_field-group mb-2 :title, class: 'form-control' %>
  </div>

  <div class="field-group mb-2">
    <%= f.label :body %>
    <%= f.text_area :body, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
