<div class="row py-3">
  <h2>Categories</h2>
  <div class="col-12">
    <% @posts.each do |post| %>
      <%= render partial: 'shared/post', locals: {post: post} %>
    <% end unless @posts.blank?  %>
  </div>
</div>
